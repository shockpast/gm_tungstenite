# examples

## discord.lua

simple discord "bot" to receive messages that bot hears in channels, doesn't have an actual heartbeat system, so that's up to user to implement

## echo.lua

echo client that sends message to echo server every 3 seconds with incrementing counter